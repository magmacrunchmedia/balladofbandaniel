// integration.js - Previously contained bridge functions
// handleInteraction() and updateInteractionPrompt() removed
// All interaction logic now lives in interactions.js (the authoritative version)
